<canvas id="hero-canvas" aria-hidden="true"></canvas>

<script>
  import { startHeroCanvas } from '../scripts/hero-canvas';

  const canvas = document.getElementById('hero-canvas');
  if (canvas instanceof HTMLCanvasElement) startHeroCanvas(canvas);
</script>

<style>
  canvas {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
  }
</style>
